===============================================================================
JavaScript Scope, Invocation Context and Inheritance
===============================================================================

All of the slides are executable JavaScript for node. To install `npm install`.
To go through the whole talk run with `vim 0*.js`.

